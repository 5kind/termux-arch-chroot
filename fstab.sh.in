m4_include(fstab-helpers)

main() {
  while read line; do
    src=$(echo "$line" | awk '{print $1}')
    dest=$(echo "$line" | awk '{print $2}')
    try_create_dest
  done < <(grep -vE '^\s*(#|$)' /etc/fstab)
  mount -a -v
}

main
